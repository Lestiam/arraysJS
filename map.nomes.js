const nomes = ["ana Julia", "Caio vinicius", "BIA silva"];

const nomesPadronizados = nomes.map((nome) => nome.toLocaleUpperCase()); //quando só temos uma função, não precisamos usar nem o return e nem as chaves

console.log(nomesPadronizados);